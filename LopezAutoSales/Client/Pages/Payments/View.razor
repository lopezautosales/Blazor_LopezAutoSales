@page "/app/payments/{id:int}"
@attribute [Authorize(Roles = "Admin")]
@inject AuthHttp auth
@inject IJSRuntime js

@if (account is null)
{
    <p class="lead">Loading...</p>
}
else
{
    <h1 class="text-center">Payments for @account.Sale.Car.Name()</h1>
    <h2 class="h4 mb-0">@account.Sale.Buyers()</h2>
    <p class="text-muted">VIN @account.Sale.Car.VIN</p>
    @if (account.IsRepossessed)
    {
        <div class="alert alert-warning no-print">Repossessed on @account.RepossessedDate?.ToShortDateString() — the remaining balance was written off.</div>
    }
    <div class="row">
        <div class="col-12 col-sm-6">
            <p class="fw-semibold mb-1">Down Payment: @account.Sale.DownPayment.ToString("C")</p>
        </div>
        <div class="col-12 col-sm-6">
            <p class="fw-semibold mb-1">Purchase Date: @account.Sale.Date.ToShortDateString()</p>
        </div>
    </div>
    <p class="lead">Initial Due: @account.InitialDue.ToString("C")</p>
    <h2 class="h4">Payments</h2>
    <div class="table-responsive">
        <table class="table table-sm table-hover table-bordered table-striped text-center">
            <thead>
                <tr>
                    <th scope="col">
                        Date
                    </th>
                    <th scope="col">
                        Amount
                    </th>
                    <th class="no-print" scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (Payment payment in account.Payments.OrderBy(x => x.Date))
                {
                    <tr>
                        <td>
                            @payment.Date.ToShortDateString()
                        </td>
                        <td>
                            @payment.Amount.ToString("C")
                            @if (!string.IsNullOrEmpty(payment.StripePaymentIntentId))
                            {
                                <span class="badge text-bg-warning ms-1" title="Recorded automatically from a Stripe online payment">Online</span>
                            }
                        </td>
                        <td class="no-print">
                            @{ string id = $"editPaymentModal{payment.Id}"; }
                            <button class="btn btn-secondary" data-bs-toggle="modal" data-bs-target="#@id" @onclick="() => OpenEdit(payment)">Edit</button>
                            <div class="modal fade" id="@id" tabindex="-1" role="dialog" aria-hidden="true">
                                <div class="modal-dialog" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title">Edit Payment</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                        </div>
                                        <div class="modal-body">
                                            <div class="mb-3">
                                                <label class="form-label w-100">Original Payment Date:
                                                    <input type="text" value="@payment.Date.ToShortDateString()" readonly class="form-control" />
                                                </label>
                                            </div>
                                            <div class="mb-3">
                                                <label class="form-label w-100">Original Payment Amount:
                                                    <input type="number" value="@payment.Amount" readonly class="form-control" />
                                                </label>
                                            </div>
                                        </div>
                                        <div class="modal-body">
                                            <div class="mb-3">
                                                <label class="form-label w-100">New Payment Date:
                                                    <input type="date" @bind-value="editPayment.Date" class="form-control" />
                                                </label>
                                            </div>
                                            <div class="mb-3">
                                                <label class="form-label w-100">New Payment Amount:
                                                    <input type="number" @bind-value="editPayment.Amount" class="form-control" />
                                                </label>
                                            </div>
                                            <div class="mb-3">
                                                <label class="form-label w-100">Reason:
                                                    <input type="text" @bind-value="editPayment.Reason" class="form-control" />
                                                </label>
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-danger" @onclick="async () => await Delete(payment, id)">Delete</button>
                                            <button type="button" class="btn btn-success" @onclick="async () => await Save(payment, id)">Save</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <EditForm Model="payment" OnValidSubmit="SubmitPayment">
        <div class="no-print row pt-5">
            <div class="col-6 col-md-3">
                <label for="PaymentDate">Date:</label>
                <InputDate class="form-control text-center" placeholder="Payment date..." id="PaymentDate" @bind-Value="payment.Date" />
            </div>
            <div class="col-6 col-md-3">
                <label for="PaymentAmount">Amount</label>
                <InputNumber class="form-control text-center" placeholder="Payment amount..." id="PaymentAmount" @bind-Value="payment.Amount" />
            </div>
            <div class="text-end mt-auto col">
                <button class="btn btn-secondary" type="submit" disabled="@_busy">
                    @if (_busy)
                    {
                        <span class="spinner-border spinner-border-sm me-1"></span>
                    }
                    Submit
                </button>
            </div>
        </div>
    </EditForm>
    <div class="text-end fw-bold">
        Balance: @account.Balance().ToString("C")
    </div>
    <div class="table-responsive mt-5 no-print">
        <table class="table table-striped text-center">
            <thead>
                <tr>
                    <th>
                        Contract Ends
                    </th>
                    <th>
                        Late Due
                    </th>
                    <th>
                        Monthly Payments
                    </th>
                    <th>
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        @account.ExpirationDate().ToShortDateString()
                    </td>
                    <td>
                        @* Clamped like the collections grid — paid-ahead shows current, not a credit. *@
                        @Math.Max(0m, account.LateDue()).ToString("C")
                    </td>
                    <td>
                        @account.MonthlyPayment.ToString("C")
                    </td>
                    <td>
                        <div class="btn-group no-print" role="group">
                            <button class="btn btn-secondary" type="button" @onclick="Print"><i class="mdi mdi-printer"></i> Print</button>
                            <a class="btn btn-secondary" href="/app/papers/view/@account.SaleId" target="_blank">View Papers</a>
                            <a class="btn btn-secondary" href="/app/papers/edit/@account.SaleId" target="_blank">Edit Papers</a>
                        </div>
                        @if (!account.IsRepossessed)
                        {
                            <button class="btn btn-outline-danger ms-2 no-print" type="button" @onclick="Repossess">Mark Repossessed</button>
                        }
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
}


@code {
        [Parameter]
        public int Id { get; set; }
    private Account account;
    private Payment payment = new Payment();
    private Payment editPayment = new Payment();
    private bool _busy;

    protected override async Task OnInitializedAsync()
    {
        account = await auth.Client.GetFromJsonAsync<Account>($"/api/account/{Id}");
        if (account != null)
        {
            payment.AccountId = Id;
            editPayment.AccountId = Id;
            payment.Amount = editPayment.Amount = account.MonthlyPayment;
        }
        await base.OnInitializedAsync();
    }

    // Seed the shared edit model from the row being opened so the dialog shows that
    // payment's real values (not a stale 0001-01-01 default carried over from another row).
    private void OpenEdit(Payment payment)
    {
        editPayment = new Payment { AccountId = Id, Id = payment.Id, Date = payment.Date, Amount = payment.Amount };
    }

    private async Task SubmitPayment()
    {
        if (account.Balance() - payment.Amount < 0 && !(await js.InvokeAsync<bool>("confirm", "Balance will be negative. Are you sure?")))
            return;
        _busy = true;
        try
        {
            var response = await auth.Client.PostAsJsonAsync("/api/account/payment", payment);
            if (response.IsSuccessStatusCode)
            {
                payment.Id = int.Parse(await response.Content.ReadAsStringAsync());
                account.Payments.Add(payment);
                payment = new Payment { AccountId = Id, Amount = account.MonthlyPayment };
            }
            else
                // Money-losing failure mode if silent: staff assume the payment saved.
                await js.InvokeVoidAsync("alert", $"The payment was NOT saved: {await response.Content.ReadAsStringAsync()}");
        }
        finally
        {
            _busy = false;
        }
    }

    private async Task Delete(Payment payment, string id)
    {
        string online = !string.IsNullOrEmpty(payment.StripePaymentIntentId)
            ? "\n\nThis payment was recorded automatically from an ONLINE (Stripe) payment. Deleting it here does NOT refund the customer, and the books will no longer match Stripe."
            : "";
        if (!await js.InvokeAsync<bool>("confirm", $"Delete the {payment.Amount.ToString("C")} payment dated {payment.Date.ToShortDateString()}?{online}\n\nThis can't be undone."))
            return;
        editPayment.Id = payment.Id;
        var response = await auth.Client.PostAsJsonAsync($"/api/account/payment/delete", editPayment);
        if (response.IsSuccessStatusCode)
        {
            account.Payments.Remove(payment);
            await js.InvokeVoidAsync("toggleModal", id);
        }
        else
            await js.InvokeVoidAsync("alert", $"The payment was NOT deleted: {await response.Content.ReadAsStringAsync()}");
    }

    private async Task Save(Payment payment, string id)
    {
        if (!string.IsNullOrEmpty(payment.StripePaymentIntentId)
            && !await js.InvokeAsync<bool>("confirm", "This payment was recorded automatically from an online (Stripe) payment. Editing it will make the books disagree with Stripe. Continue?"))
            return;
        editPayment.Id = payment.Id;
        var response = await auth.Client.PutAsJsonAsync($"/api/account/payment", editPayment);
        if (response.IsSuccessStatusCode)
        {
            payment.Date = editPayment.Date;
            payment.Amount = editPayment.Amount;
            editPayment = new Payment { AccountId = Id, Amount = account.MonthlyPayment };
            await js.InvokeVoidAsync("toggleModal", id);
        }
        else
            await js.InvokeVoidAsync("alert", $"The changes were NOT saved: {await response.Content.ReadAsStringAsync()}");
    }

    private async Task Repossess()
    {
        if (!await js.InvokeAsync<bool>("confirm", $"Mark this account as REPOSSESSED for {account.Sale.Buyers()} ({account.Sale.Car.Name()})?\n\nThe remaining balance of {account.Balance().ToString("C")} will be written off and the account will drop off the payments list. This can't be undone here."))
            return;
        var response = await auth.Client.PostAsync($"/api/account/repossess/{Id}", null);
        if (response.IsSuccessStatusCode)
        {
            account.IsRepossessed = true;
            account.RepossessedDate = DateTime.Now;
        }
        else
            await js.InvokeVoidAsync("alert", await response.Content.ReadAsStringAsync());
    }

    private async Task Print()
    {
        await js.InvokeVoidAsync("print");
    }
}
